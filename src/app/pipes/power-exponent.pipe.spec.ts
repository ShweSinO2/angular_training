import { PowerExponentPipe } from './power-exponent.pipe';

describe('PowerExponentPipe', () => {
  it('create an instance', () => {
    const pipe = new PowerExponentPipe();
    expect(pipe).toBeTruthy();
  });
});
